<div class="flickr">
  <ul>
    <% photos.each do |p| %>
      <li>
          <%= link_to image_tag(FlickRaw.url_b(p), class: 'flickr_photo', title: p.title, border: 0, size: '75x75'), FlickRaw.url_short(p) %>
      </li>
    <% end %>
  </ul>
</div>